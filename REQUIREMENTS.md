# strategy-lab — 要件定義書

**バージョン**: 1.0
**最終更新**: 2026-07-23
**目的**: 就活・採用担当者向けショーケース兼、実運用移行を視野に入れた高精度アルゴリズムトレーディング戦略ポートフォリオ

---

## 1. プロジェクト概要

### 1.1 目的

クオンツ・リスク管理職種への応募を意図した技術ポートフォリオ。単一戦略の実装ではなく、**複数戦略を統一的なバックテスト・評価基盤の上で比較検証できるフレームワーク**を提示することで、以下を採用担当者に示す：

- 戦略設計・検証の方法論に対する理解
- Look-ahead bias・過学習など、バックテストの落とし穴に対する認識
- 確率過程・統計的手法（Exam P、Shreve『Stochastic Calculus for Finance』の学習内容）の実装への応用力
- 機関投資家的なリスク管理の視点（ロング/ショート、ポジションサイジング、取引コスト考慮）

### 1.2 非目的（Out of Scope）

- 高頻度取引・レイテンシ依存戦略（執行インフラの制約上、リテール環境での実運用は非現実的）
- ディープラーニングベースの価格予測（過学習リスクの説明責任が重く、本プロジェクトの目的に合わない）
- risk-engineリポジトリとの直接連携（本プロジェクトは評価指標も含め完全に自己完結させる）

### 1.3 成功基準

- [ ] 2つのコア戦略（ペアトレード、マルチファクター）が完全に動作し、S&P500ユニバースでバックテスト可能
- [ ] Look-ahead biasのない、時系列を厳密に分離したバックテストエンジン
- [ ] 取引コストを織り込んだ、現実的なパフォーマンス評価
- [ ] README単体で採用担当者が5分以内に「何をした人か」を理解できる
- [ ] コードは第三者が`git clone`後、数コマンドで再現可能

---

## 2. 技術スタック

| 項目 | 選定 |
|---|---|
| 言語 | Python 3.11+ |
| 開発環境 | VSCode + ターミナル上のClaude Code |
| データ取得 | yfinance（無料API）。将来的な有料データ差し替えを見越し、データ取得層を抽象化 |
| バックテストエンジン | 自前実装（vectorbt/backtrader等の既存ライブラリは使わず、設計力を示すため） |
| 数値計算 | numpy, pandas, scipy, statsmodels |
| 統計検定 | statsmodels（ADF検定、Johansen検定）、arch（必要に応じてGARCH） |
| 可視化 | matplotlib, seaborn（静的レポート）、plotly（インタラクティブ、余力があれば） |
| テスト | pytest、TDD前提、カバレッジ目標90%+ |
| CI/CD | GitHub Actions、codecov連携 |
| 依存管理 | poetry（依存関係の厳密なロック） |
| リポジトリ名 | `strategy-lab` |

---

## 3. リポジトリ構成

```
strategy-lab/
├── README.md                      # プロジェクト概要、結果ハイライト、再現手順
├── REQUIREMENTS.md                # 本ドキュメント
├── pyproject.toml                 # poetry管理
├── .github/workflows/ci.yml       # テスト・カバレッジ自動実行
│
├── core/
│   ├── data/
│   │   ├── loader.py               # yfinance抽象化層（データソース差し替え可能に設計）
│   │   ├── universe.py             # S&P500構成銘柄取得・管理
│   │   └── cache.py                # ローカルキャッシュ（API制限対策）
│   ├── backtest/
│   │   ├── engine.py                # コアバックテストエンジン（look-ahead防止の時系列分離）
│   │   ├── execution.py             # 約定シミュレーション（timing, slippage, commission）
│   │   ├── portfolio.py             # ポジション管理、ポートフォリオ評価額の時系列追跡
│   │   └── position_sizing.py       # Kelly基準 / ボラティリティターゲティング
│   ├── evaluation/
│   │   ├── metrics.py                # Sharpe, Sortino, Calmar, max drawdown等
│   │   ├── statistical_tests.py      # リターンの統計的有意性検定
│   │   └── report.py                 # パフォーマンスレポート自動生成
│   └── risk/
│       ├── var_es.py                  # VaR/Expected Shortfall（自前実装、risk-engineとは独立）
│       └── exposure.py                # ロング/ショートのグロス・ネットエクスポージャー管理
│
├── strategies/
│   ├── base.py                       # 全戦略共通の抽象基底クラス（Strategy Interface）
│   ├── pairs_trading/
│   │   ├── cointegration.py           # Engle-Granger / Johansen検定によるペア選定
│   │   ├── kalman_hedge.py            # 動的ヘッジ比率推定（Kalmanフィルタ）
│   │   ├── static_hedge.py            # 静的OLSヘッジ比率（比較対象）
│   │   ├── signal.py                  # z-scoreベースのエントリー/エグジット
│   │   └── strategy.py                # 戦略本体（Strategy Interfaceを実装）
│   ├── factor_momentum/
│   │   ├── factors.py                 # モメンタム（12-1）、低ボラティリティファクター定義
│   │   ├── portfolio_construction.py  # ファクタースコアに基づくロング/ショート構築
│   │   └── strategy.py
│   └── vol_arbitrage/                 # 拡張枠（初期スコープ外、設計のみ用意）
│       └── README.md                  # 実装未着手の理由と設計方針を明記
│
├── notebooks/
│   ├── 01_pairs_trading_analysis.ipynb
│   ├── 02_factor_momentum_analysis.ipynb
│   └── 03_strategy_comparison.ipynb   # 2戦略の横比較（採用担当者向けの見せ場）
│
├── reports/                          # バックテスト結果の自動生成レポート（HTML/PDF）
│
└── tests/
    ├── core/
    └── strategies/
```

---

## 4. バックテストエンジン要件（共通基盤）

実運用可能な精度を謳うための必須要件。risk-engine開発時の反省（Student-t ESの符号誤り、Kupiec POFのゼロ違反時の扱い、rolling lookaheadガード）を踏まえ、以下を厳格に実装する。

### 4.1 Look-ahead bias防止

- 全ての特徴量・シグナル計算は、**その時点で入手可能だった情報のみ**を使用することを保証する設計にする
- ローリングウィンドウ計算において、`t`時点の値を計算する際に`t`より未来のデータを一切参照しないことをテストで担保する
- コインテグレーション検定のようなペア選定自体も、**in-sample期間で選定し、out-of-sample期間で検証**する分離を必須とする（全期間データでペアを選ぶのはlook-ahead biasの典型的な誤り）

### 4.2 取引コストモデル

- 手数料：固定bps（デフォルト想定値は要検討、後述）
- スリッページ：固定bpsモデルを基本とし、出来高連動モデルをオプションで用意
- 空売りコスト：ショートポジションの借株コスト（stock borrow rate）を簡易モデルで考慮するかは要検討

### 4.3 約定シミュレーション

- デフォルト：シグナル発生日の**翌日始値約定**（当日終値約定は look-ahead的な楽観バイアスになりやすいため避ける）
- タイミング仮定はコンフィグで明示的に切り替え可能にする

### 4.4 ポジションサイジング

- ボラティリティターゲティングを基本方式とする（Kelly基準は理論値が過大になりやすく、実務では half-Kelly 等の縮小が一般的。両方実装し比較できるようにする）

### 4.5 リスク管理

- グロス・ネットエクスポージャーの上限設定
- 銘柄あたり・セクターあたりの集中度上限
- ポートフォリオレベルのVaR/ESモニタリング（自前実装）

---

## 5. 戦略要件

### 5.1 ペアトレード（統計的裁定）

**ペア選定**
- S&P500構成銘柄からセクター内ペアを候補に絞り込み（業種の異なる銘柄同士のペアは経済的合理性が薄いため除外）
- Engle-Granger検定（2段階法）とJohansen検定の両方を実装し、結果を比較できるようにする
- 選定は in-sample期間のみで実施し、out-of-sample期間で有効性を検証

**ヘッジ比率推定**
- 静的OLS（ベースライン）
- 動的Kalmanフィルタ（時変ヘッジ比率、確率過程の知識を実装に反映する見せ場）

**シグナル**
- スプレッドのz-scoreに基づくエントリー（例：|z| > 2でエントリー、z → 0でエグジット、要バックテストでチューニング）
- half-life（スプレッドの平均回帰速度）を算出し、保有期間の妥当性を検証する

### 5.2 マルチファクター（モメンタム＋低ボラティリティ、拡張でバリュー等）

**ファクター定義**
- モメンタム：12ヶ月リターンから直近1ヶ月を除外した12-1モメンタム（academic literatureの標準的定義）
- 低ボラティリティ：過去60営業日のリターンボラティリティ

**ポートフォリオ構築**
- ファクタースコアに基づき上位/下位パーセンタイルでロング/ショート（機関投資家的設計）
- セクター中立化（セクターエクスポージャーを相殺し、純粋なファクターエクスポージャーを取り出す）は拡張要件として検討

**リバランス**
- 月次リバランスを基本とする（週次は取引コストが嵩みすぎ、実運用可能性の観点で不利）

### 5.3 ボラティリティ裁定（拡張枠）

初期スコープ外。設計方針のみ`strategies/vol_arbitrage/README.md`に記載し、「なぜ今回実装しなかったか（オプションデータ調達コストの制約）」を明記する。これ自体が「スコープ管理ができる」というアピールになる。

---

## 6. 評価指標

- リターン系：累積リターン、年率リターン、月次/年次リターン分布
- リスク調整後：Sharpe比、Sortino比、Calmar比
- ドローダウン：最大ドローダウン、ドローダウン期間
- 統計的有意性：リターンがゼロと有意に異なるかのt検定、ブートストラップによる信頼区間
- ベンチマーク比較：S&P500との比較（アルファ・ベータ）
- 戦略間比較：2戦略（＋将来のvol_arbitrage）を同一期間・同一評価基準で横並び比較するレポート

---

## 7. 確定事項・残論点

### 7.1 確定事項

- **依存管理**：poetry
- **サバイバーシップバイアス対応**：Level A（現在のS&P500構成銘柄のみを使用）を採用。README・レポートに「既知の限界（limitation）」として明記し、バックテスト成績が実際よりやや楽観的に出ている可能性を隠さない。Level Bへの拡張余地はある旨も記載する
- **取引コスト**：手数料0.5bps（片道）、スリッページ5〜10bps（片道、流動性の高いS&P500銘柄を想定）をデフォルト値とする。コンフィグで変更可能にし、感度分析（コストを2倍・3倍にした場合のパフォーマンス劣化）をレポートに含めることで、コスト前提への頑健性を示す
- **バックテスト対象期間**：2015年〜現在（約10年）。コロナショック（2020）、その後の利上げ局面（2022〜）など複数の市場レジームを含む

### 7.2 残論点（実装フェーズで決定）

1. **セクター中立化の実装要否**（マルチファクター戦略）
2. **ショート借株コストのモデル化要否**
3. **レポート出力形式**：HTML静的レポート vs Jupyter notebook中心 vs 両方

---

## 8. マイルストーン（叩き台）

| フェーズ | 内容 |
|---|---|
| Phase 0 | リポジトリ初期化、CI/CD設定、データ取得層・キャッシュ実装 |
| Phase 1 | バックテストエンジン（core/backtest/）実装、TDD、look-ahead防止のテスト整備 |
| Phase 2 | 評価指標モジュール（core/evaluation/）実装 |
| Phase 3 | ペアトレード戦略実装・検証 |
| Phase 4 | マルチファクター戦略実装・検証 |
| Phase 5 | 戦略比較レポート、README整備、可視化仕上げ |
| Phase 6（拡張） | vol_arbitrage設計書 or 実装、セクター中立化等の高度化 |

---

## 9. 次のアクション

要件定義は完了。以下の順でPhase 0に着手する。

1. `strategy-lab`リポジトリをGitHub上に作成（ローカルでpoetry初期化 → git init → remote追加）
2. VSCode + ターミナルでClaude Codeを起動し、本REQUIREMENTS.mdを読み込ませた上でPhase 0（データ取得層・CI/CD設定）から着手
3. Claude Codeへの初期プロンプトでは、本ドキュメントのセクション4（バックテストエンジン要件）を特に厳格に守らせる（look-ahead bias防止のテストを先に書かせるTDDアプローチを推奨）
